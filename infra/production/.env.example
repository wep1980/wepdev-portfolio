# Nenhuma variável externa é necessária para esta primeira etapa.
# O serviço define NODE_ENV, PORT e HOSTNAME diretamente no compose.yaml.
#
# Não versionar arquivos .env reais com tokens, senhas, credenciais do
# Cloudflare ou qualquer outro segredo operacional.
